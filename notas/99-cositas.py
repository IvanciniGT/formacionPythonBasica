
# En python puedo generar un número aleatorio entre 2 valores dados
import random # La libreria random (que es una librería que viene de serie en python)

numero_aleatorio = random.randint(1, 10) # Genera un número aleatorio entre 1 y 10 (ambos incluidos)
print("El número aleatorio generado es: " + str(numero_aleatorio))